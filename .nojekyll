Tells GitHub Pages to serve this folder as-is instead of running it through
Jekyll. Without it, Pages can skip files and folders whose names begin with an
underscore, and builds are slower for no benefit. Harmless on every other host.
